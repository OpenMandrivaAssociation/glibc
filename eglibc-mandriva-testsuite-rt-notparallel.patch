--- libc/rt/Makefile.tsp~	2012-10-25 19:18:12.000000000 +0200
+++ libc/rt/Makefile	2012-12-27 20:05:26.757814339 +0100
@@ -81,3 +81,13 @@ $(tests:%=$(objpfx)%-bp): $(objpfx)librt
 endif
 
 tst-mqueue7-ARGS = -- $(host-built-program-cmd)
+
+# XXX avoid timing issues on fine-grained SMT systems
+ifeq (powerpc, $(base-machine))
+no-parallel-testing = yes
+endif
+ifneq ($(filter %tests,$(MAKECMDGOALS)),)
+ifeq ($(no-parallel-testing),yes)
+.NOTPARALLEL:
+endif
+endif
